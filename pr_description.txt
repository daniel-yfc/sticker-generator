🧹 [Extract Button component to remove code duplication]

🎯 What: Extracted the duplicated button styles into a shared `Button` component (`components/Button.tsx`) and refactored `ResultDisplay`, `StickerSetView`, `Gallery`, `ImageEditor`, and `App` to use it.
💡 Why: Improves maintainability by centralizing the button styling. It reduces code duplication and allows for easier global updates to the primary button style across the application.
✅ Verification: Ran the Vitest test suite and ensured that all tests pass. Created a unit test for the new `Button` component. Built the app using `npm run build` with no errors.
✨ Result: Replaced duplicate Tailwind class strings with a clean, reusable `<Button>` component with proper variants.
